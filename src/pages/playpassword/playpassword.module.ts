import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PlaypasswordPage } from './playpassword';

@NgModule({
  declarations: [
    PlaypasswordPage,
  ],
  imports: [
    IonicPageModule.forChild(PlaypasswordPage),
  ],
})
export class PlaypasswordPageModule {}
